# Working copies kept while the design direction was being decided
build-handoff/
build-mine-backup/

# Claude Design canvas scaffolding — not part of the shipped app
_ds/
*.dc.html
support.js
ios-frame.jsx
.thumbnail

# Loose screenshots and pasted images from the design conversation
messageimage_*.jpg
screenshot-*.png
logo-*.png

# OS / editor
Thumbs.db
desktop.ini
.DS_Store

# Unpacked copy of the zip — the zip and the single-file build are enough
dist/horapatana-build/

# Full mirror of horapatana.com (272 MB, 352 images incl. the whole Zyro media
# library). Kept locally as a reference/backup — this repo is public, and the
# library holds far more than the site actually shows.
archive/
